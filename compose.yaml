services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - laya-data:/data
      - laya-models:/models
    restart: unless-stopped
    init: true

volumes:
  laya-data:
  laya-models:
