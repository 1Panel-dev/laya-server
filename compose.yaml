services:
  app:
    image: 1panel/laya-server:${LAYA_IMAGE_TAG:-latest}
    env_file: .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - laya-data:/data
    restart: unless-stopped
    init: true

volumes:
  laya-data:
